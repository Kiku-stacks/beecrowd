#include <stdio.h>
#include <stdlib.h>
 
int main() {
    
    int h1,m1,h2,m2,minutoTotais,minutoFinal,minutoInicial;
    scanf("%d%d%d%d",&h1,&m1,&h2,&m2);

    while(h1!=0 || h2!=0 || m1!=0 || m2!=0){
        if(h2 - h1 == 0){
            minutosTotais = 1440;
        }

        if(h2 < h1){
            minutoInicial = h1*60+m1
            minutoFinal = h2*60+m2;

            
        }

        
        
    }
 
    return 0;
}